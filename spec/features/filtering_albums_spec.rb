require "rails_helper"

RSpec.feature "Filtering albums" do
  context "by genre" do
    it "with default title sorting"
    it "with reversed title sorting"
    it "with year sorting"
    it "with year sorting reversed"
  end

  context "by year" do
    it "with default title sorting"
    it "with reversed title sorting"
    it "with genre"
    it "with genre reversed"
  end
end
