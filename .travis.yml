language: ruby

cache: bundler

rvm: 
  - 2.2.4

addons:
  postgresql: "9.5" 

services:
  - postgresql

before_script:
  - psql -c 'create database travis_ci_test;' -U postgres
  - cp config/database.yml.travis config/database.yml

script:
  - bundle exec rspec
