<div class="row">
  <div class="col-sm-9">
    <div class="well">
      <%= form_for artist, html: { class: "form-horizontal" } do |f| %>
        <% if artist.errors.any? %>
          <ul class="list-group">
            <% artist.errors.full_messages.each do |msg| %>
              <li class="list-group-item list-group-item-danger"><%= msg %></li>
            <% end %>
          </ul>
        <% end %>

        <div class="form-group <%= has_error?(artist, :name) %>">
          <%= f.label :name, class: "col-sm-2 control-label" %>
          <div class="col-sm-9">
            <%= f.text_field :name, class: "form-control", placeholder: "The Foobars" %>
          </div>
        </div>

        <div class="form-group">
          <%= f.label :wikipedia, class: "col-sm-2 control-label" %>
          <div class="col-sm-9">
            <%= f.text_field :wikipedia, class: "form-control", placeholder: "The_Foobars" %>
          </div>
        </div>

        <div class="form-group">
          <%= f.label :website, class: "col-sm-2 control-label" %>
          <div class="col-sm-9">
            <%= f.text_field :website, class: "form-control", placeholder: "http://www.thefoobars.com" %>
          </div>
        </div>

        <div class="form-group">
          <%= f.label :description, class: "col-sm-2 control-label" %>
          <div class="col-sm-9">
            <%= f.text_area :description, rows: 7, class: "form-control" %>
          </div>
        </div>

        <div class="form-group">
          <div class="col-sm-offset-2 col-sm-9">
            <%= link_to "Cancel", back_link, class: "btn btn-default btn-sm" %>
            <%= f.submit "Submit", class: "btn btn-success btn-sm" %>
          </div>
        </div>
      <% end %>
    </div>
  </div>
</div>
